import classNames from 'classnames'
import React from 'react'

export default function DeleteButton({removeCheck, item, activeId}) {
  return (
    <button className="hide-button todo__list-remove-button button">Удалить задачу</button>
  )
}
